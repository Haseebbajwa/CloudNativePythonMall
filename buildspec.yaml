version: 0.2

# env:
#   shell: shell-tag
#   variables:
#     key: "value"
#     key: "value"
#   parameter-store:
#     key: "value"
#     key: "value"
#   exported-variables:
#     - variable
#     - variable
#   secrets-manager:
#     key: secret-id:json-key:version-stage:version-id
#   git-credential-helper: no | yes

        
phases:
  install:
    commands:
  build:
    commands:
       - docker build -t haseebpython .
       - docker save -o haseebpython.tar haseebpython
#      - echo "Logging in to Docker Hub"
 #     - echo "ishaatmta6896366" | docker login -u "hasahm" --password-stdin

  #    - echo "Building Docker image"
   #   - docker build -t hasahm/stefanpython .

    #  - echo "Pushing Docker image to Docker Hub"
     # - docker push hasahm/stefanpython
 artifacts:
   files:
     - 'testimage.tar'
   name: testimage_haseeb
